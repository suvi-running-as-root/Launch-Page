# For local development, the backend's address. For a single-service deployment,
# omit this so the site calls its own /api routes.
VITE_API_URL=http://127.0.0.1:4000
VITE_ZEFFY_DONATE_URL=https://your-zeffy-form-url
